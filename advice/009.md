# C3c 执行记录 —— Three-Layer Feasibility Frontier（依据 advice/005.md §十九）

C3b 四层对照完成后，执行 **C3c 三层 feasibility frontier**，把 "INFEASIBLE"
拆成 physical / policy-family / registered-grid 三种明确判定，极大提高论文
理论可信度（005 §十九）。

## 1. L1 Physical feasibility —— PASS

预算内最大 evidence 能否达 QoS？N=8、H=96、b=16 下最多 4×8-bit（4×24=96
恰满预算）。用 MITM 精确全融合 ROC（P_D,max^det-thr，005 §九 口径）评估：

| 配置 | cost | P_MD | ≤β=0.40 |
| --- | --- | --- | --- |
| 4 最强 SNR UAV 8-bit | 96 | 0.1072 | YES |
| 4 最强 SNR UAV 4-bit | 80 | 0.1093 | YES |
| 4 最弱 SNR UAV 8-bit | 96 | 0.3633 | YES |

**物理层可行**（T45 锁定；MITM 确定性，无 seeds）。注：MITM 是 4-UAV 子集
精确融合（H=96 最多 4×8-bit，其余 UAV 不发 = 0 evidence），与 C2.1 的
π_full 构造（4×8-bit=96=H）同构。

## 2. L2 Policy-class feasibility —— PASS（deterministic）

deterministic + randomized convex hull 能否达 QoS？升级 C3a 的点估计
convex_hull_diag 为 **Wilson U95 口径**（mixture 的 violation 计数线性
kfa(λ)=λkfa_a+(1−λ)kfa_b，同一 worlds 决策独立，再对 mixture 计数用 U95
判定——与正式 Gate 一致，007 审计指出的点估计≠U95 问题在此闭环）：

- deterministic feasible：**4/4**（Phase-PJ、Myopic-PJ、Myopic-All、
  Direct8 的 θ̂=(256,0.8) 点 U95 均 FEASIBLE）
- 两两 randomized mixture（U95）进入 QoS 象限：**6/6 对**
- **policy-class feasible（deterministic）**——C3b 的可行方法已覆盖，
  mixture 冗余但方向一致

## 3. L3 Controller-search feasibility —— 4/5

当前有限 (ρ,η) 网格是否找到？直接引用 C3b 校准：

- Phase-PJ/Myopic-PJ/Myopic-All/Direct8：θ̂=(256,0.8)、feasible 8-10/28
  ⇒ **registered-grid feasible**
- StaticProg：0/28 ⇒ **registered-grid infeasible**

## 4. 三层归因（005 §十九）

| 层 | 判定 | 含义 |
| --- | --- | --- |
| L1 Physical | PASS | 物理层可行（改预算/成本无必要） |
| L2 Policy-class | PASS | 算法层可行（policy family 达标） |
| L3 Controller-search | 4/5 | StaticProg 是 registered-grid 不够，**非物理/算法层** |

**关键结论**：StaticProg 的 0/28 NO 已明确归因为 registered-grid/policy
层——不是物理不可行（L1 证明 4×8-bit 可达），也不是整体算法不可行（L2
证明其他方法 policy-class feasible）。三层归因闭环，C3b 的 "StaticProg
无 FEASIBLE" 从此有了精确定位。

## 5. 验证

- **test_regressions.py：59/59 PASS**（新增 T45 C3c L1 physical
  feasibility）
- 报告：`report/MVS-C_C3c_report.md`（FULL 564.6s）+ smoke 版
- README：C3c 完成 bullet + 运行命令 + 目录登记
